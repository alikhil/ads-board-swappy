@using Swappy_V2.Models
@model IEnumerable<DealModel>
@{
    ViewBag.Title = "Мои объявления";
}

<h2>Управление объявлениями</h2>

<div class="dl-horizontal">
    @if (Model != null)
    {
        foreach (DealModel deal in Model)
        {
            Html.RenderPartial("ShowDeal", deal, new ViewDataDictionary { { "Manage", true } });

        }
    }
    @if (Model.Count() == 0)
    {
        <div class="alert alert-info" role="alert">У вас еще нет объявлений. Вы можете создать их @Html.ActionLink("здесь", "Create")</div>
    }
    <div id="tooltip"></div>
</div>